import React from 'react';

function Layout() {
  return (
    <section>
        <aside>aside</aside>
        <section>
            <header>header</header>
            <main>main</main>
        </section>
    </section>
    );
}

export default Layout;
